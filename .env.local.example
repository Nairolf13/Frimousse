## Local environment example for Frimousse
# Copy to .env.local or .env and fill in secret values. Do NOT commit real secrets.

# Backend
DATABASE_URL="postgresql://user:password@localhost:5432/DBname"
JWT_SECRET="replace_with_secret"
REFRESH_TOKEN_SECRET="replace_with_secret"
API_URL=http://localhost:4000

# SUPABASE: fill these with your project's values. Keep SERVICE_ROLE_KEY secret and only in server envs.
SUPABASE_URL=https://YOUR_SUPABASE_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace_with_service_role_key
SUPABASE_BUCKET=PrivacyPictures
# Optional fallback: secret used to sign short-lived JWTs for server-side uploads
SUPABASE_JWT_SECRET=replace_with_supabase_jwt_secret

# Frontend Vite variables (prefix VITE_)
VITE_SUPABASE_URL=https://YOUR_SUPABASE_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=replace_with_anon_key
VITE_SUPABASE_BUCKET=PrivacyPictures

# Other examples
FRONTEND_URL=http://localhost:5173

# Do not commit real secrets. Use a secrets manager in production (or pm2 ecosystem with env vars).
